# PostgreSQL connection string from Supabase or Neon.
# Example:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
DATABASE_URL=

# Use `require` for managed cloud PostgreSQL like Neon / Supabase.
# Use `disable` only for local PostgreSQL without SSL.
PGSSL=require
